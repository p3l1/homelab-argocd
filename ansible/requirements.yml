---
collections:
  # Nicht auf Galaxy veroeffentlicht - Installation direkt aus dem Repo.
  - name: https://github.com/k3s-io/k3s-ansible.git
    type: git
    version: "1.2.2"
  - name: community.general
    version: ">=7.0.0"
  - name: community.sops
  - name: ansible.posix
    version: ">=1.5.0"
