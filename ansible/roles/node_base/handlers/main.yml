---
- name: Restart ssh
  ansible.builtin.systemd:
    name: ssh
    state: restarted

# Der Adresswechsel trennt die Verbindung. Nach dem Neustart antwortet der
# Node unter der Adresse aus dem Inventory.
- name: Reboot for network change
  ansible.builtin.reboot:
    reboot_timeout: 300
